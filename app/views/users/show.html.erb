<%= render partial: "partials/header" %>
<main id="users-show">
	  <div class="user_name"><%=@user.name%>のページ</div>
  	<ul>
	  	<% @user.posts.each do |post| %>
	    <li>
			  <div class="img" style="background-image: url(<%= post.image.to_s %>)"></div>
			</li>
	  	<% end %>
	  </ul>
</main>
<%= render partial: "partials/footer" %>